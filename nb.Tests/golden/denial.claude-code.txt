=== ladder-miss / shell / Bash
--- ledger
deny / no-match
--- model
The user doesn't want to proceed with this tool use. The tool use was rejected. STOP what you are doing and wait for the user to tell you how to proceed.

(Bash (Read): /etc/passwd — nothing in the approval policy allows it.)
--- human (stderr)
[nb] denied: Bash (Read): /etc/passwd — nothing in the approval policy allows it.
       authorize with: approval bash cat *

=== ladder-miss / write-outside-cwd / Write
--- ledger
deny / no-match
--- model
The user doesn't want to proceed with this tool use. The tool use was rejected. STOP what you are doing and wait for the user to tell you how to proceed.

(Write → /etc/motd — nothing in the approval policy allows it.)
--- human (stderr)
[nb] denied: Write → /etc/motd — nothing in the approval policy allows it.
       No approval directive grants paths outside the working directory. Work within it, or start nb from a directory that contains the target.

=== ladder-miss / network-fetch / WebFetch
--- ledger
deny / no-match
--- model
The user doesn't want to proceed with this tool use. The tool use was rejected. STOP what you are doing and wait for the user to tell you how to proceed.

(WebFetch: https://example.com/data.json — nothing in the approval policy allows it.)
--- human (stderr)
[nb] denied: WebFetch: https://example.com/data.json — nothing in the approval policy allows it.
       authorize with: approval fetch allow

=== ladder-miss / read-outside-cwd / Read
--- ledger
deny / no-match
--- model
The user doesn't want to proceed with this tool use. The tool use was rejected. STOP what you are doing and wait for the user to tell you how to proceed.

(Read → /etc/hostname — nothing in the approval policy allows it.)
--- human (stderr)
[nb] denied: Read → /etc/hostname — nothing in the approval policy allows it.
       No approval directive grants paths outside the working directory. Work within it, or start nb from a directory that contains the target.

=== default-deny / shell / Bash
--- ledger
deny / default-deny
--- model
The user doesn't want to proceed with this tool use. The tool use was rejected. STOP what you are doing and wait for the user to tell you how to proceed.

(Bash (Read): /etc/passwd — the approval policy default is deny, and no allow-rule matched.)
--- human (stderr)
[nb] denied: Bash (Read): /etc/passwd — the approval policy default is deny, and no allow-rule matched.
       authorize with: approval bash cat *

=== default-deny / write-outside-cwd / Write
--- ledger
deny / default-deny
--- model
The user doesn't want to proceed with this tool use. The tool use was rejected. STOP what you are doing and wait for the user to tell you how to proceed.

(Write → /etc/motd — the approval policy default is deny, and no allow-rule matched.)
--- human (stderr)
[nb] denied: Write → /etc/motd — the approval policy default is deny, and no allow-rule matched.
       No approval directive grants paths outside the working directory. Work within it, or start nb from a directory that contains the target.

=== default-deny / network-fetch / WebFetch
--- ledger
deny / default-deny
--- model
The user doesn't want to proceed with this tool use. The tool use was rejected. STOP what you are doing and wait for the user to tell you how to proceed.

(WebFetch: https://example.com/data.json — the approval policy default is deny, and no allow-rule matched.)
--- human (stderr)
[nb] denied: WebFetch: https://example.com/data.json — the approval policy default is deny, and no allow-rule matched.
       authorize with: approval fetch allow

=== default-deny / read-outside-cwd / Read
--- ledger
deny / default-deny
--- model
The user doesn't want to proceed with this tool use. The tool use was rejected. STOP what you are doing and wait for the user to tell you how to proceed.

(Read → /etc/hostname — the approval policy default is deny, and no allow-rule matched.)
--- human (stderr)
[nb] denied: Read → /etc/hostname — the approval policy default is deny, and no allow-rule matched.
       No approval directive grants paths outside the working directory. Work within it, or start nb from a directory that contains the target.

