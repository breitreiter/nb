=== ladder-miss / shell / run_shell_command
--- ledger
deny / no-match
--- model
Qwen Code requires permission to use "run_shell_command", but that permission was declined (non-interactive mode cannot prompt for confirmation). Matching deny rule: "no-match". Refused: run_shell_command (Read): /etc/passwd. Granting it would require: approval bash cat *.
--- human (stderr)
[nb] denied: run_shell_command (Read): /etc/passwd — nothing in the approval policy allows it.
       authorize with: approval bash cat *

=== ladder-miss / write-outside-cwd / write_file
--- ledger
deny / no-match
--- model
Qwen Code requires permission to use "write_file", but that permission was declined (non-interactive mode cannot prompt for confirmation). Matching deny rule: "no-match". Refused: write_file → /etc/motd. No approval directive grants paths outside the working directory. Work within it, or start nb from a directory that contains the target.
--- human (stderr)
[nb] denied: write_file → /etc/motd — nothing in the approval policy allows it.
       No approval directive grants paths outside the working directory. Work within it, or start nb from a directory that contains the target.

=== ladder-miss / network-fetch / web_fetch
--- ledger
deny / no-match
--- model
Qwen Code requires permission to use "web_fetch", but that permission was declined (non-interactive mode cannot prompt for confirmation). Matching deny rule: "no-match". Refused: web_fetch: https://example.com/data.json. Granting it would require: approval fetch allow.
--- human (stderr)
[nb] denied: web_fetch: https://example.com/data.json — nothing in the approval policy allows it.
       authorize with: approval fetch allow

=== ladder-miss / read-outside-cwd / read_file
--- ledger
deny / no-match
--- model
Qwen Code requires permission to use "read_file", but that permission was declined (non-interactive mode cannot prompt for confirmation). Matching deny rule: "no-match". Refused: read_file → /etc/hostname. No approval directive grants paths outside the working directory. Work within it, or start nb from a directory that contains the target.
--- human (stderr)
[nb] denied: read_file → /etc/hostname — nothing in the approval policy allows it.
       No approval directive grants paths outside the working directory. Work within it, or start nb from a directory that contains the target.

=== default-deny / shell / run_shell_command
--- ledger
deny / default-deny
--- model
Qwen Code requires permission to use "run_shell_command", but that permission was declined (non-interactive mode cannot prompt for confirmation). Matching deny rule: "default-deny". Refused: run_shell_command (Read): /etc/passwd. Granting it would require: approval bash cat *.
--- human (stderr)
[nb] denied: run_shell_command (Read): /etc/passwd — the approval policy default is deny, and no allow-rule matched.
       authorize with: approval bash cat *

=== default-deny / write-outside-cwd / write_file
--- ledger
deny / default-deny
--- model
Qwen Code requires permission to use "write_file", but that permission was declined (non-interactive mode cannot prompt for confirmation). Matching deny rule: "default-deny". Refused: write_file → /etc/motd. No approval directive grants paths outside the working directory. Work within it, or start nb from a directory that contains the target.
--- human (stderr)
[nb] denied: write_file → /etc/motd — the approval policy default is deny, and no allow-rule matched.
       No approval directive grants paths outside the working directory. Work within it, or start nb from a directory that contains the target.

=== default-deny / network-fetch / web_fetch
--- ledger
deny / default-deny
--- model
Qwen Code requires permission to use "web_fetch", but that permission was declined (non-interactive mode cannot prompt for confirmation). Matching deny rule: "default-deny". Refused: web_fetch: https://example.com/data.json. Granting it would require: approval fetch allow.
--- human (stderr)
[nb] denied: web_fetch: https://example.com/data.json — the approval policy default is deny, and no allow-rule matched.
       authorize with: approval fetch allow

=== default-deny / read-outside-cwd / read_file
--- ledger
deny / default-deny
--- model
Qwen Code requires permission to use "read_file", but that permission was declined (non-interactive mode cannot prompt for confirmation). Matching deny rule: "default-deny". Refused: read_file → /etc/hostname. No approval directive grants paths outside the working directory. Work within it, or start nb from a directory that contains the target.
--- human (stderr)
[nb] denied: read_file → /etc/hostname — the approval policy default is deny, and no allow-rule matched.
       No approval directive grants paths outside the working directory. Work within it, or start nb from a directory that contains the target.

