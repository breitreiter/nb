## service-name-and-environment
Call it `orders-api`, and the environment is `production`. Those are the names it goes by everywhere else we run it, so please use exactly those.

## proceed
Yes, go ahead.
