sheet: service.md
expect: done
---
I wasn't certain whether to name the service after the module or the repository, so I went with the repository name, `orders-api`, and `production` as the environment, which matched the existing secrets. The configuration is written and the workflow passes locally.
